server:
  port: 7076

spring:
  application:
    name: auth-service

  security:
    oauth2:
      client:
        registration:
          keycloak:
            client-id: auth-service-client
            client-secret: m5sM1pRaOmbIDPvbJEaX3FPGyo77BgxU
            authorization-grant-type: authorization_code
            scope: openid
            # redirect-uri: http://localhost:7076/login/oauth2/code/keycloak
        provider:
          keycloak:
            issuer-uri: http://keycloak:8080/realms/gestion-etudiants

management:
  endpoints:
    web:
      exposure:
        include: health,info
